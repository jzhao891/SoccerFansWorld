# dependencies
node_modules
/.pnp
.pnp.*

# next.js
.next/
/out/
next-env.d.ts

# expo
.expo/
dist/

# turbo
.turbo/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (contains API keys — never commit)
.env
.env*.local

# one-time setup docs (contains account details — keep local only)
ONETIME_SETUP.md

# seed script logs
/logs/*.log

# venue seed data (contains Google Places IDs — keep local only)
backend/resources/venues*.json

# Firebase Admin SDK service-account key — SECRET, NEVER commit
serviceAccountKey.json
*serviceAccount*.json
*service-account*.json

# typescript
*.tsbuildinfo
.vercel
.env*
# ...but DO commit env templates (no secrets — just documents required vars)
!.env.example
!**/.env.example

# claude local notes (machine-specific, never commit)
CLAUDE.local.md
